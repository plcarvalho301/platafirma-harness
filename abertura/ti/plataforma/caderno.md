# caderno — plataforma (ti)

## Conhecimento curado

- Promover código de uma unit de usuário (systemd --user) NÃO basta com `release promover`: ele materializa `current` e recria containers de STACK, mas não recarrega units de usuário. O systemd exige `systemctl --user daemon-reload` quando o `.service` mudou no disco, e `infra restart` não o faz — reexecuta o ExecStart mas o systemd mantém a definição velha em memória. Sintoma: unit segue no código antigo após promover + restart pela porta; o journal não registra start novo. Prova é medir o efeito, não confiar no "reiniciada: active" do verbo. Cura hoje é ato no host (fora da porta): `sudo -u <conta> XDG_RUNTIME_DIR=/run/user/$(id -u <conta>) systemctl --user daemon-reload && ...restart <unit>`. Mesma classe do pf-porta-watch que não reinicia a porta após promover.

- Sonda de leitura que passa por `sessao abrir` cunha chave viva no msg-mem como efeito colateral — leitura com escrita escondida. Prova de disponibilidade de montagem deve usar leitura pura (`expediente montar --sem-acervo`), nunca `sessao abrir`/`monta-sessao`. Padrão a imitar: sondas infra_estado/conferir_* do agregador não cunham nada.

- Stack de código (harness) e código servido pela PORTA sobem por caminhos diferentes: stack por `deploy promover` (recria container), verbo servido pela porta pela promoção do harness + reinício da porta. Mudança em bin/<verbo> só chega ao que a sessão executa quando o harness é promovido; até lá a porta serve o código velho, mesmo com o PR já em main. Não confundir "mergeado em main" com "servido".

- Um mecanismo dormente (gancho no-op guardado "caso precise") que já provou levar a deriva é dívida, não conveniência: mantê-lo deixa disponível o desvio que causou o problema. O pre_build (andaime da wiki, #3075) era isso — removido quando nenhuma stack o usava. Régua: gancho que só existe para um caso já eliminado sai junto com o caso.

- Novo `oauth2-proxy-<sufixo>` atrás da borda não fica pronto só com compose+override: o client Keycloak `oauth2-proxy` é COMPARTILHADO por todos os gates (mesmo clientId, mesmo secret) e guarda `redirectUris` num array próprio, fora do git — editável só por `seg keycloak -- update clients/<id> -r platafirma -s 'redirectUris=[...]'` (acha o id com `seg keycloak -- get clients -r platafirma -q clientId=oauth2-proxy`). Esquecer esse passo dá `Invalid parameter: redirect_uri` na tela do Keycloak com o container do gate já saudável e a rota já certa — sintoma engana, não é DNS nem túnel. Também: `docker restart` NÃO recarrega bind de volume novo (mount fica o de quando o container foi criado); mudou volume, precisa recriar: `deploy <stack> up -d --no-deps --force-recreate <servico>`.

## Diário de bordo

2026-09-20 — card #3089, estancar cunhagem do agregador: promovi 169ef4d (release promover, gate verde) e a sonda seguiu cunhando ~1 chave-sonda/cadeira/min. Tentei `infra restart harness-agregador` 2x — retornou "reiniciada: active" com warning "changed on disk — run daemon-reload", mas o journal não registrou start novo e `sessao limpar --dry-run` seguiu contando sondas novas. Causa: unit de usuário não recarrega por `release promover` nem `infra restart`; systemd exige daemon-reload, sem verbo servido para ele. Contorno NA DATA: o dono rodou no host `sudo -u claudinho XDG_RUNTIME_DIR=/run/user/$(id -u claudinho) systemctl --user daemon-reload && ...restart`; depois disso, tick > 45s = 0 sondas novas. Encaminhado à mesa #19 (ampliar `infra restart` para dar daemon-reload quando a unit mudou no disco).

2026-09-20 — card #3098 (limpeza pre_build): `deploy conhecimento promover --ensaio` recusou com Permission denied de wiki/montar-wiki, script já deletado em main — o campo pre_build do registro acervo.stack ainda apontava o script morto e o ensaio rodava contra a árvore de release velha. `release promover platafirma-conhecimento --ensaio` recusou (família de código exige rev explícita); `release promover ... <sha> --ensaio` recusou (ensaio não faz fetch do espelho). `repo atualizar platafirma-harness` recusou exit 3 (ramo de fábrica sem upstream — nada a puxar). Contornos NA DATA: para o registro, `acervo stack import` de um JSON de uma linha com pre_build:null (nota: o import lê a chave `_nota`, não `nota`); para o SHA, `repo git <repo> fetch origin main` antes de resolver o ref; para o promover, rodar sem --ensaio (o real faz o fetch do espelho). Ao editar o bin/deploy, write_file no path /home/claudinho/AI/platafirma-harness editou clone irmão, não o worktree wt/platafirma-harness/ti que o `repo git` usa (diff vazio) — refiz no path do `rev-parse --show-toplevel`. test_deploy.sh (bash) não roda por `teste` (só pytest neste repo); validei por lint + leitura.

2026-09-23 — card #3129, fiação de casa.platafirma.org: gate `oauth2-proxy-casa` + rota + site placeholder (PR #1 em platafirma-core, promovido). Duas armadilhas na ativação, nenhuma no PR em si: (1) `docker restart` não recarrega bind de volume novo — o container seguiu com os mounts de quando foi criado e repetiu `authenticated-emails-casa.txt: no such file` até eu recriar com `deploy core up -d --no-deps --force-recreate oauth2-proxy-casa`; (2) depois disso, Keycloak devolveu `Invalid parameter: redirect_uri` — o client `oauth2-proxy` é compartilhado pelos 5 gates e o array `redirectUris` não é tocado por `release promover`/`deploy promover`, precisou de `seg keycloak -- update clients/<id> ...` à parte. Migração do site de verdade (arq:0115 §1-11) fica com dados/Olga — avisada na fila 20260923T131448-ti.
