2026-09-21 — faxina de git dos 8 repos. `repo sanear` opera SO o wt/ti da sessao (deu 'limpo -- nada a fazer' enquanto havia worktrees pendurados e branches gone em outros lugares). Contorno NA DATA: git cru pela porta — `worktree list`/`worktree remove` (sem --force, que auto-protege worktree sujo), `branch -D`, `push origin --delete`. Poda de remotas com seguranca: `branch -r --merged origin/main` (ancestrais) + `git cherry origin/main <b>` (pega squash-merged por patch-id, que --merged nao ve); apagar so as 100%% em main, preservar qualquer com commit proprio/PR/main/wip. APRENDIZADO: `worktree remove` tira SO o diretorio — a branch e os commits ficam; nunca perde commit, e --force so afeta mudanca nao-commitada. Clones-mae presos em ramo de fabrica NAO sao alcancaveis por `repo git` (ele opera o wt/ti da cadeira).