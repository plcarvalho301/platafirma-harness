# caderno — release (chapéu de TI)

## Lições (conhecimento curado)

- Promover platafirma-harness NÃO é `deploy` nem `release promover ... --ensaio` sem rev: é família de código, exige rev explícita alcançável por origin/main (`release promover platafirma-harness <sha>`). `deploy` só serve stacks de compose (harness-controle, chat, etc.), não o harness inteiro.
- Merge de PR não sobe código: o release fica no sha anterior até `release promover`. Sequência de entrega de PR da fábrica = pr-merge → repo atualizar (pega o sha novo do squash) → release promover <sha> → a própria promoção despacha o restart da porta (ops-mcp), destacado. Conferir depois com `infra estado ops-mcp` + `infra saude`.
- Validar entrega da fábrica exige o venv do release (`/opt/platafirma/current/venv/harness`), não o Python do sistema nem `uvx`. As suítes de contrato (controle/tests, ops-server) só passam nesse venv; fora dele quebram por dep faltando, sem ser culpa do código.
- `release promover <repo> <sha>` atualiza o CÓDIGO dos verbos (a porta serve a rev nova), mas NÃO recria os containers das stacks daquele repo — cada stack sobe separado por `deploy <stack> promover`, que faz o build. Uma mudança em `bin/<verbo>` sobe com `release promover harness`; uma mudança que precisa de container novo (wiki, rag, core) exige o `deploy` da stack DEPOIS. Foi o que separou "gate morto e no ar" (release harness bastou) de "wiki não subiu" (faltava o deploy da stack conhecimento, que ainda por cima falhou no build).
- Gate de confirmação por env var (`PF_SIM=1 deploy ...`) é INALCANÇÁVEL pela porta só-verbo: a porta faz `execve` puro, sem shell, e não há onde prefixar variável de ambiente. Confirmação de gate crítico só é passável por dentro da porta se for ARGUMENTO (`--sim`), não env var. Regra geral para desenhar gate novo: a confirmação viaja na chamada, nunca no ambiente.

## Diário de bordo

2026-09-18 — validar PR 63 rodando testes: `teste rodar /tmp/pr63` recusou (só aceita slug dentro de ~/AI); worktree solto (`/home/claudinho/AI/pr63-harness`) rodou com uvx/python3.14 do sistema e quebrou por yaml/redis/mcp/psycopg ausentes — falsos vermelhos de ambiente. Contorno NA DATA 18/09: apontei o worktree oficial `wt/platafirma-harness/ti` para o ramo do PR (checkout --detach), rodei `teste rodar platafirma-harness <alvo>` (usa o venv harness), 4 suítes de contrato verdes; depois restaurei o worktree para eed707a→main.
2026-09-18 — suíte ops-server/test_run_command_lote_injecao.py não coleta nem no venv harness: `ModuleNotFoundError: mcp` (server.py:64, import da FastMCP, fora do diff). Parede: o venv harness não carrega o pacote `mcp` da porta. Contorno NA DATA 18/09: nenhum daqui; validado por leitura do diff do teste + relato do autor (3/3 no ambiente da porta). Fica registrado que essa suíte precisa do venv `ops`, não `harness`.
2026-09-18 — `git` cru, `bash`/`ls`/`find` crus e `cd &&` recusados pela porta (roteados a repo/teste/read_file/descobrir, ou barrados como metacaractere). Contorno NA DATA 18/09: git via `repo git <slug> -C <path> ...`; navegação de arquivo por `run_command` com `cwd`; nunca encadear com `&&`.
2026-09-18 — `release promover ... --ensaio` para sha recém-mesclado falhou: "rev não resolve no forge" — o espelho do release não faz fetch em ensaio. Contorno NA DATA 18/09: rodar a promoção real (ela faz o fetch), com o sha completo confirmado por `repo git rev-parse origin/main`.
2026-09-19 — editar bin/deploy: `write_file` com caminho relativo (bin/deploy) recusou (fora de morada); escrevi no clone base /home/claudinho/AI/platafirma-harness/bin/deploy e o `repo commitar` disse "nada a commitar" — o `repo` opera no worktree da cadeira wt/platafirma-harness/ti, nao no clone base. Contorno NA DATA 19/09: refazer todas as edicoes com caminho absoluto no worktree wt/platafirma-harness/ti; conferir com `repo diff` que o worktree ve as mudancas antes de commitar.
2026-09-19 — `PF_SIM=1 deploy conhecimento promover` inalcancavel: gate por env var, porta faz execve sem shell. Tentei `deploy` direto e `run_command` "deploy ...": os dois batem no mesmo gate, nao ha onde injetar a env. Contorno NA DATA 19/09: nenhum pela porta — a correcao foi matar o gate no codigo (ordem do dono), nao contornar.
2026-09-19 — `repo empurrar faxina:main` rejeitado (fetch first): origin/main avancou desde a criacao do ramo. Contorno NA DATA 19/09: `repo <slug> fetch origin main` (ato git, NAO repetir a palavra git nos args), depois `repo <slug> rebase origin/main`, depois empurrar. Cuidado: passar sessao_id errado faz o `repo` cair no fallback do clone base (aviso PF_CADEIRA nao definida) e ver arvore suja de outro lugar.
2026-09-19 — `teste rodar platafirma-harness testes/test_deploy.sh` nao roda .sh: o verbo teste so coleta pytest ("no tests ran"), e `bash ...` pela porta recusa (sem verbo, sugestao null = verbo que falta). Parede: nao ha runner de teste shell na porta. Contorno NA DATA 19/09: validei o bin/deploy por `lint` (bash limpo) + revisao de diff; a suite shell fica sem execucao pela porta (registrado como limite no card #3075 para a fabrica rodar por bash no ambiente do repo).
2026-09-19 — `tarefas api-corpo PATCH /items/3075` com stdin JSON: o parser da tool interpreta a string JSON como objeto e rejeita ("Input should be a valid string"). Parede: nao consegui reescrever o corpo (description) do card pela escotilha. Contorno NA DATA 19/09: pus o corpo executavel para a fabrica como COMENTARIO (`tarefas comentar`, que aceita texto cru), nao como description do item.
2026-09-19 — `release promover platafirma-harness fe33f15` retornou erro de execucao da tool no meio da chamada: a promocao do proprio harness reinicia a porta (ops-mcp), que serve a tool. Nao era falha: reconferido com `release estado` que a promocao pegou (fe33f15 no ar, porta reiniciada). Contorno NA DATA 19/09: apos promover o harness, sempre reconferir por `release estado` em vez de confiar no retorno da chamada, que pode morrer com o restart.