# Dependências de runtime do verbo `pesquisar` (spec §6; dono desta linha: ia).
#
# O verbo roda pelo venv harness da release (shebang de bin/pesquisar aponta para
# /opt/platafirma/current/venv/harness/bin/python — mesmo venv de fila, mesa e monta-sessao).
# Esse venv é construído do lockfile na promoção (/opt é r-x): estas linhas entram no lock
# do venv harness, e ninguém instala à mão nele nem no system python. Depois da promoção,
# uma vez por host:
#     /opt/platafirma/current/venv/harness/bin/crawl4ai-setup   # baixa o Chromium do Playwright (~600 MB, em ~/.cache; RAM zero em repouso)
#
# crawl4ai já embute playwright, bs4 e lxml. O venv harness já traz httpx (usado
# por outros verbos); fica explícito porque searxng.py e os resolvedores dependem dele.
crawl4ai>=0.9
httpx>=0.27

# Só para a suíte de contrato/conformidade:
pytest>=8.2
