# Dependências de runtime do verbo `pesquisar` (spec §6; dono desta linha: ia).
#
# O verbo roda pelo .venv-harness (shebang de bin/pesquisar aponta para
# ~/AI/.venv-harness/bin/python — mesmo venv de fila, mesa e monta-sessao).
# Instale AÍ, não no system python nem no ~/AI/.venv (que o verbo não usa):
#     uv pip install --python ~/AI/.venv-harness/bin/python -r pesquisa/requirements.txt
#     ~/AI/.venv-harness/bin/crawl4ai-setup   # baixa o Chromium do Playwright (~600 MB; RAM zero em repouso)
#
# crawl4ai já embute playwright, bs4 e lxml. O .venv-harness já traz httpx (usado
# por outros verbos); fica explícito porque searxng.py e os resolvedores dependem dele.
crawl4ai>=0.9
httpx>=0.27

# Só para a suíte de contrato/conformidade:
pytest>=8.2
